<div class="col m9"><br>
  <center><h1 class="white-text"> All Stores </h1></center>
  <div class="card">
    <table class="bordered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Status</th>
          <th colspan="1"></th>
        </tr>
      </thead>
      <tbody>
        <% @stores.each do |store| %>
        <tr id="store-<%= store.id %>">
          <td><%= link_to store.name, platform_admin_store_path(store.id) %></td>
            <td> <%= store.status %> </td>
          <td>
            <% if store.status == "Active" %>
              <%= button_to "Deactivate", platform_admin_store_path(store.id), method: :put %>
            <% else %>
              <%= button_to "Activate", platform_admin_store_path(store.id), method: :put %>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
